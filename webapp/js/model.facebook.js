function Facebook() {
	var self = this;

	self.connectToFriend = function(callback) {
		// TODO: implement this
		// Call callback with friend JSON object.
		// Call callback with null if no one is found
		callback(null);
	};

	self.connectToRandom = function(callback) {
		// TODO: implement this
		// Call callback with friend JSON object.
		// retry until one is found.
		callback(null);
	};
}